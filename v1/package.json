{
  "name": "v1",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp start.sh dist/ && zip -r dist.zip dist/  ",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.4.5"
  },
  "dependencies": {
    "@mediapipe/face_mesh": "^0.4.1633559619",
    "three": "^0.155.0"
  }
}
